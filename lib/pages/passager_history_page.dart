import 'package:flutter/material.dart';

class PassagerHistoryPage extends StatelessWidget {
  PassagerHistoryPage({Key key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}