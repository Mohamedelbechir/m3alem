import 'package:flutter/material.dart';

class DriverMoneyPage extends StatelessWidget {
  DriverMoneyPage({Key key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: <Widget>[Text("je suis mohaed el")],
      ),
    );
  }
}
