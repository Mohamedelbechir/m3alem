import 'package:flutter/material.dart';

class DriverSettingPage extends StatelessWidget {
  DriverSettingPage({Key key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}